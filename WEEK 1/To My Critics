#include<iostream>
#include<vector>
#include<algorithm>
#include<cmath>
using namespace std;
typedef long long ll;

int main() {
    int t;
    cin>>t;
    while(t--){
        vector<int> A(3,0);
        cin>>A[0]>>A[1]>>A[2];
        sort(A.begin(),A.end());
        if(A[1]+A[2]>=10) cout<<"YES"<<endl;
        else cout<<"NO"<<endl;
    }
}
